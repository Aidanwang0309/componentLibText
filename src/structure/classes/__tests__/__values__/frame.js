test.skip("skip", () => {});
export const defaultWidth = {
  isConstrained: false,
  value: null,
};
export const defaultInsets = {
  top: null,
  left: null,
  bottom: null,
  right: null,
};

export const defaultFrame = {
  width: defaultWidth,
  insets: defaultInsets,
};
